{
  "dependencies": {
    "concurrently": "^7.4.0",
    "express": "^4.18.1",
    "json": "^11.0.0",
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "create": "npx create-react-app client && cp example-template/App.js client/src/ && mkdir server && cp example-template/index.js server/ && json -I -f client/package.json -e \"this.proxy1='http://localhost:3005'\"",
    "client": "npm start --prefix ./client",
    "server": "nodemon server/index.js",
    "start": "concurrently -k 'npm:client' 'npm:server'",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
